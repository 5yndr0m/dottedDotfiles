function fish_greeting
    # Color for readability
    # printf '\e[38;5;250m'

    # # Fetch and display quote
    # echo
    # set_color yellow

    # set response (curl -s https://programming-quotesapi.vercel.app/api/random)
    # # Parse JSON safely
    # set quote (echo $response | jq -r '.en + " — " + .author' 2>/dev/null)
    # if test -z "$quote"
    #     set quote '"Code is like humor. When you have to explain it, it’s bad." — Cory House'
    # end
    # echo "$quote"

    # set_color normal
end
